export enum ERouterLink {
  home = '/',
  notfound = '*',
  login = '/login',
  product = '/product',
  productId = '/product/:id',
  categoryId = '/categories/:category',
  search = '/search',
  searchItem = 'search/:search'
}
